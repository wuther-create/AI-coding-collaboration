# 提示词模板

> `{花括号}` 是你要替换的内容。默认**先出方案、后写代码**；方案确认了再动手。
>
> 仓库里**没有可参照的同类实现**时，先看**模板 0**；否则从模板 1 往下选。

**速查**

| 场景 | 模板 |
|---|---|
| 全新仓库 / 第一次做这类功能 | 0 |
| 小改动（1–3 个文件） | 1 |
| 中型功能 | 2 |
| 大型功能（跨模块/跨服务） | 3 |
| 已有部分实现，要补全 | 4 |
| 跨多个需求章节 / 多个仓库 | 5 |
| 多数据库 / 多平台 / 多版本 | 6 |
| **启动编码** | 7 |
| 单个阶段执行 | 8 |
| 处理 blocker | 9 |
| 收尾复查 | 10 |
| 紧急问题排查 | 11 |
| 性能优化 | 12 |
| 架构调优 / 重构 | 13 |

---

## 模板 0：从零开始（仓库无参照物）

> 适用：全新仓库、或这个功能方向在仓库里是第一次做。

```text
我要做的是：{一句话描述}
现状：{全新仓库 / 有骨架但没做过这类功能 / 有代码但缺设计稿}

请先不要写功能代码，分两步走。

第一步，给我一份「锚点说明」，回答：
1. 这属于哪种「零」：全新仓库 / 新类型功能 / 缺同类实现 / 缺设计稿；
2. 你打算用的替代锚点是什么（官方脚手架 / 当前版本官方文档 /
   团队其他仓库 / 成熟开源实现），每项标明来源、版本、选它的理由；
3. 哪些地方**没有可靠锚点、属于你的推断**——单独列出来，我来确认；
4. 这个方向上常见的坑和已废弃的做法有哪些，当前版本的推荐做法是什么。

第二步，等我看过锚点说明后再执行：
1. 用官方脚手架生成最小可运行骨架，不要手写目录结构；
2. 跑通构建 + 测试 + 启动；
3. 写一份最小约定文档：结构、命名、错误处理、配置、测试放哪；
4. 停下来让我 review 骨架和约定；
5. 我确认后，再按阶段实现功能。

硬性要求：
- 引入的每个外部范式都必须标注来源，不能写得像是仓库既有约定；
- 边界除了「做什么」，还要写「本期不引入什么」（中间件、抽象层、设计模式）；
- 不确定的地方写「待确认」，不要用看起来合理的写法填空；
- 骨架和功能分两次提交，不要一次生成整个项目。
```

---

## 模板 1：小型改动（轻量流程）

```text
任务：{一句话描述要做的事}

请你：
1. 先读 {相关文件/目录}，并找到仓库里最相似的已有实现，读一遍；
2. 用不超过 15 行说明你的实施计划：改哪些文件、每个文件做什么、
   边界是什么（明确不做什么）；
3. 我看过之后再动手。

规则：
- 不新建文件，除非确实必要（必要就说明理由）；
- 路径和命名沿用仓库现有习惯，不要发明；
- 遇到与现有代码冲突的地方，先告诉我，不要自行决定。
```

---

## 模板 2：中型功能（先出方案）

```text
请根据《{需求/设计文档路径}》中的「{功能名称}」部分，
生成一份实施方案，输出到 {你指定的路径}。

要求：
1. 先读 {项目说明文件} 与该需求文档；
2. 搜索仓库中同类功能的实现，方案里的路径必须是搜到过的；
3. 明确写出「本期不做什么」；
4. 需求与现有代码冲突时列为 blocker，给出 2-3 个选项及代价，不要静默选择；
5. 覆盖所有受影响的区域（{如：服务端 / 客户端 / 数据库 / 配置 / 测试}）；
6. 按依赖顺序划分阶段，每个阶段写明允许修改范围和禁止修改范围。

先输出方案，我确认后再进入编码。
```

---

## 模板 3：大型功能（完整方案）

```text
请根据《{需求/设计文档路径}》的 {章节/范围}，
生成完整实施方案，输出到 {你指定的路径}。

背景：{涉及哪些模块/服务，为什么要做，有哪些已知约束}

要求：
1. 先读项目说明、需求文档、以及仓库中已有的同类方案文档（若有），对齐粒度；
2. 需求文档很长，只定位目标部分，不要做全文摘要；
3. 先做「现状盘点」：列出这个功能相关的已有实现，区分【新增】【修改】【保留】【删除】；
4. 列出所有 blocker 与待确认决策，每条给出选项、代价、建议；
5. 文件级变更清单，逐个文件写清职责、依赖、开发顺序、完成标准；
6. 对外契约（接口/事件/数据格式）要写明兼容性影响与迁移方式；
7. 数据变更要给出回滚方案；
8. 按依赖顺序划分阶段，每阶段写明允许/禁止修改范围、验证命令、卡住怎么办；
9. 完整覆盖：{列出所有受影响区域}；
10. 附执行提示词与验收清单。

先输出方案，blocker 我会逐条确认，确认后再进入编码。
```

---

## 模板 4：已有部分实现，需要补全

```text
请根据《{需求文档路径}》中的「{功能名称}」部分生成实施方案。

仓库里已经存在以下落地：
- {已有文件/模块路径}
- {已有文件/模块路径}

要求：
1. 增加「现状盘点」一节，区分【新增】【修改】【保留】【删除】；
2. 不要把已有实现当作从零新建；
3. 已有实现与需求的每一处差异，逐条列为 blocker 或校准项，不要静默沿用；
4. 说明本次改动对已有调用方的影响。
```

---

## 模板 5：跨多个需求章节 / 多个仓库

```text
请根据《{需求文档路径}》中的 {章节A}「{功能A}」与 {章节B}「{功能B}」，
合并生成一份实施方案，输出到 {路径}。

涉及仓库：{仓库1}、{仓库2}

要求：
1. 明确两部分的边界，以及共享的数据结构/接口/类型；
2. 标出跨仓库的契约变更点，说明谁先改、如何兼容；
3. 合并后仍是文件级清单，不要退化成需求摘要。
```

---

## 模板 6：多目标/多方言（多数据库、多平台、多版本）

```text
请根据《{需求文档路径}》中的「{功能名称}」生成实施方案。

本次需要同时适配：{目标A}、{目标B}（如 {数据库A} / {数据库B}，
或 {平台A} / {平台B}，或 {版本A} / {版本B}）

要求：
1. 给出各自的产出位置与命名规则：{目录约定}；
2. 说明两者的差异点（类型映射、语法、API 差异、行为差异）；
3. 标注**无法直接映射**的部分，并给出处理方式；
4. 说明如何验证两套产出都被正确覆盖。
```

---

## 模板 7：启动编码（总体提示词）

方案 review 通过后，用这段启动：

```text
你现在要基于 {方案文件路径} 实施「{功能名称}」。

执行规则：
1. 先读 {项目说明文件}、{需求文档}、{方案文件} 三份；
2. 按方案中的阶段顺序执行，不跳步、不合并阶段；
3. 不实现方案里「不做什么」清单中的内容；
4. 不新建未确认的模块、目录、依赖；
5. 遇到需求与代码冲突，立即停止并上报，不要自行拍板；
6. 每个阶段完成后按回执格式报告：变更文件、实际执行的验证命令、实际结果；
7. 遵守代码质量约定：风格与相邻代码一致、不引入新依赖、不吞异常、
   不留 TODO 和调试代码、不重写无关的既有代码、不顺手格式化整个文件、
   不改任务范围外的文件；
8. 每个阶段结束后停下等我确认，再进入下一个。

现在从第一阶段开始。
```

---

## 模板 8：单个阶段的执行提示词

```text
执行 {方案文件路径} 中的阶段 {N}：{阶段名称}。

- 只允许修改：{范围}
- 禁止修改：{范围}
- 输入：{要读的文件}
- 完成标准：{标准}

完成后按回执格式报告，不要顺手做下一阶段的事。
```

---

## 模板 9：处理 blocker

```text
你报告的 blocker「{冲突描述}」已确认，决策如下：
{选定的方案}

请：
1. 更新方案文档中受影响的章节（契约/数据/阶段范围）；
2. 在「风险与冲突」一节把该条标记为「已决策」并记录结论与理由；
3. 说明这个决策是否影响其他阶段的允许修改范围。

改完停下，不要直接进入编码。
```

---

## 模板 10：复查（方案已执行完，收尾前跑一遍）

```text
请对照 {方案文件路径} 复查本次改动：
1. 逐条核对验收清单，列出**未通过**的项，不要只报通过的；
2. 检查是否有超出方案的改动；
3. 检查是否有遗留的 TODO / 调试代码 / 临时注释；
4. 检查安全基线（{项目相关的那几条}）是否都满足；
5. 给出真实的验证命令与实际输出。

只报告事实，不要给出乐观结论。
```

---

## 模板 11：紧急问题排查

```text
现象：{观察到什么}
影响：{影响范围与严重程度}
环境：{生产/预发/测试，版本，时间点}
已有线索：{日志、监控、报错、复现步骤}

请按以下顺序协助，不要跳步：

1. 先列出还需要收集哪些证据来缩小范围（去哪看日志、盯什么指标、怎么复现），
   并说明每条证据能**排除**哪个假设；
2. 给出 2-4 个候选假设，按可能性排序，每个说明「若成立会看到什么现象」；
3. 告诉我当前能做的**可逆止血动作**有哪些，按侵入性从低到高排；
4. 等我确认后再执行，一次只做一步，做完报告观察到的结果。

硬性要求：
- 未经我确认，不修改任何代码或数据；
- 不要用吞异常、加 try/catch、注释代码、加 sleep 的方式让现象消失；
- 无法复现或无法确定根因时，直接说「无法确定」，
  不要给一个听起来合理的解释；
- 每个结论都要标明是【有证据】还是【推测】。
```

---

## 模板 12：性能优化

```text
优化对象：{接口 / 函数 / 页面 / 任务}
当前表现：{具体数字，如 P99 延迟 800ms}
目标：{期望数字}

请按「先测量，后优化」的顺序：

1. 先告诉我怎么测出可靠基线：用什么工具、什么条件、测几次、看哪些指标；
2. 我提供基线数据后，你再基于数据给出瓶颈假设和验证手段；
3. 确认瓶颈后给优化方案，**一次只改一处**；
4. 每改一处，说明预期收益与代价（复杂度 / 内存 / 一致性风险）；
5. 改完同条件复测，给出 before / after 数字对比。

硬性要求：
- 没有基线数据，不要给优化方案；
- 不优化未被测量到的地方；
- 不一次改多个变量；
- 收益不达标或代价过高时，主动建议回滚；
- 若瓶颈在别处（网络 / 数据库 / 外部服务），直接说明，不要硬优化当前代码。
```

---

## 模板 13：架构调优 / 重构

```text
重构对象：{模块 / 层次 / 组件}
重构目标：{可维护性 / 解耦 / 替换依赖 / 统一模式}
必须保持不变：{对外契约、数据格式、错误语义、时序保证}

请按以下步骤：

1. 先盘点：这块代码当前有哪些测试覆盖？哪些行为没有测试保护？
2. 列出**对外契约**清单（谁在调用、依赖什么行为），确认哪些绝对不能变；
3. 若测试不足，先给一份特征化测试方案，把现状（含看起来怪异的行为）固定下来；
4. 把重构拆成**独立的小步骤**，每步一个意图、能单独回滚、单独验证；
5. 我确认拆分后，一步一步来，每步结束停下报告。

硬性要求：
- 不改变外部可观察行为；
- 不在同一个提交里混入性能优化或格式调整；
- 不顺手改无关文件；
- 每步之后跑测试，失败立即回滚该步；
- 若发现必须改变行为，停下来告诉我，不要自己决定。
```
