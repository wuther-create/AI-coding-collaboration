# 检查清单

> 给人用的。A 是动手前查，B 是 review 方案时查，C–F 是收尾时查。

| 清单 | 什么时候用 |
|---|---|
| A. 动手前的准备 | 拿到任务、还没开工 |
| B. 方案 review 表 | AI 出了方案，你要 review |
| C. 验收清单 | 编码完成，提交前 |
| D. 常见错误对照表 | 出问题时对照排查 |
| E. 排查 / 优化 / 调优场景 | 这三类任务开工前 |
| F. 代码质量 | 提交前 |

---

## A. 动手前的准备

- [ ] 需求来源明确（文档路径或 issue 编号），目标部分能定位
- [ ] 项目说明文件存在且描述了结构约定；若没有，先补一份
- [ ] 已找到并读过仓库中最相似的已有实现（若仓库确实没有，见《场景补充.md》第 1 节，先确认替代锚点）
- [ ] 当前分支的构建/测试是**通的**（否则出问题分不清是谁的锅）
- [ ] 已知受影响的所有区域（含调用方、配置、文档、测试）
- [ ] 已知外部依赖（第三方服务、凭据、环境）
- [ ] 已知是否有原型/线框图/接口文档，位置在哪
- [ ] 若为改造类任务，已知现有实现的完整范围

## B. 方案 review 表

**按规模取用**：小规模只看前 4 行。

| 检查项 | 不通过的典型表现 |
|---|---|
| 是否有「不做什么」清单 | 缺失，或写成「其他暂不实现」 |
| 变更清单是否真实存在于仓库 | 出现没人见过的路径 |
| 契约/字段是否对齐需求 | 名称、必填项、类型与需求不一致 |
| 阶段是否有「禁止修改范围」 | 没写边界，AI 必然越权 |
| blocker 是否给了决策选项 | 只描述问题，没给可选方案 |
| 是否残留 `TBD` / `视情况` | 有模糊占位没清掉 |
| 验证命令是否具体 | 写「跑一下测试」而不是实际命令 |
| 数据变更是否有回滚方案 | 只说怎么改，不说怎么退 |
| 是否覆盖所有受影响区域 | 漏了调用方 / 配置 / 文档 / 测试 |
| 对外契约变更是否说明兼容性 | 改了接口不说谁受影响 |

## C. 验收清单

**通用（每次都查）**
- [ ] 主流程走通，成功路径结果正确
- [ ] 失败分支有明确处理，不吞异常
- [ ] 边界条件：空值、极值、超长、特殊字符
- [ ] 重复操作被正确处理（幂等或明确拦截）
- [ ] 无遗留调试代码、注释掉的代码、临时文件
- [ ] 无新增的编译/静态检查告警

**按项目类型追加**

| 项目类型 | 追加检查 |
|---|---|
| 有服务端 | 输入校验在服务端生效；权限检查在数据访问前；无 N+1 / 全表扫描 |
| 有数据库 | 变更可回滚；大表操作有评估；索引覆盖查询条件 |
| 有界面 | 加载态、错误态、空态；键盘可达；小屏可用 |
| 有对外 API | 兼容性；错误码有文档；限流生效 |
| 有并发 | 竞态、超时、重试的幂等性 |
| 有定时/后台任务 | 失败重试；重复执行安全；日志可追踪 |
| 是库/SDK | 公共 API 稳定；有使用示例；版本变更记录 |

**安全验收**
- [ ] 《CLAUDE.md》第 6 节逐条核对
- [ ] 抽样检查日志无敏感信息
- [ ] 新增配置项无不慎提交的凭据

**AI 返工预防**
- [ ] 所有阶段的允许/禁止范围都填写了
- [ ] 没有阶段被 AI 自行合并或跳过
- [ ] 每个阶段的验证命令都有可复现的记录
- [ ] 实际改动与方案一致（跑一次《提示词模板.md》模板 10）

## D. 常见错误对照表

| 错误 | 现象 | 修正 |
|---|---|---|
| 只说要做什么，没说要读什么 | AI 凭空发明目录结构 | 提示词里写明需求文档路径 + 「先读项目说明」 |
| 冲突被静默处理 | 方案直接选了一条路，掩盖了与现有代码的矛盾 | 要求输出「冲突 + 选项 + 代价」，由人决策 |
| 一次 prompt 干完全部 | 出问题无法定位，改一处崩三处 | 拆成有允许/禁止范围的阶段 |
| 阶段没有禁止范围 | AI 越权改了不相关文件 | 检查每阶段的「禁止修改范围」 |
| 没验收就进下一阶段 | 错误累积到最后一起来 | 每阶段结束 review 回执再放行 |
| 方案写成需求摘要 | 只有章节复述，没有文件级落点 | 输出文件级清单、契约、阶段、验收标准 |
| 拿大流程做小改动 | 半天写方案，十分钟改代码 | 用《CLAUDE.md》第 0 步的规模判断降档 |
| 直接进入编码 | 方案没 review 就让 AI 写 | 先人工 review，再启动编码 |
| 验证靠 AI 自述 | 说"已通过"但没跑过 | 要求贴实际命令与实际输出 |

## E. 排查 / 优化 / 调优场景

这三类场景另有一套**更严**的纪律（详见《场景补充.md》第 2 节）。动手前逐条对：

**通用**
- [ ] 我手里是**证据**，不是猜测
- [ ] 这一步**能回滚**
- [ ] 一次只改了**一个变量**
- [ ] 有**客观数字或测试**证明这一步有效

**紧急排查**
- [ ] 没有删除或覆盖现场证据
- [ ] 没有用吞异常 / 注释代码 / 加 sleep 的方式让现象消失
- [ ] 每个动作都记录了「改了什么 → 观察到什么」
- [ ] 事后补了能提前发现该问题的监控或测试

**性能优化**
- [ ] 有 before / after 的具体数字和环境
- [ ] 优化的是**测量到**的瓶颈，不是猜的
- [ ] 代价（复杂度 / 内存 / 一致性）已说明
- [ ] 优化后完整测试通过，行为等价

**架构调优**
- [ ] 对外契约未变（或已标注为破坏性变更并经确认）
- [ ] 每步都有测试保护，且每步可单独回滚
- [ ] diff 干净，没混入格式调整或无关文件

## F. 代码质量

详见《CLAUDE.md》第 8 节。提交前逐条对：

- [ ] 新代码风格与相邻文件一致，没引入自己的风格
- [ ] 每个调用的 API / 符号都已核实存在
- [ ] 没有引入未说明的新依赖
- [ ] 错误处理去掉后**会**影响行为（不是噪音）
- [ ] 没有吞异常、没有静默返回默认值
- [ ] 没有过度防御（内部调用不加无意义判空 / 兜底）
- [ ] 没有过度抽象（一个用例不造接口和工厂）
- [ ] 没有注释掉的代码、TODO、调试打印
- [ ] 没有顺手改动任务范围外的文件
- [ ] 没有整个文件格式化造成的 diff 污染
- [ ] 新逻辑有测试；bug 修复有复现测试
- [ ] 把实现改坏，测试真的会失败
- [ ] 一个提交一个意图，能单独回滚
