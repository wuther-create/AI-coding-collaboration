# 场景补充：从零开始 / 排查 / 优化 / 调优

> 本文件是《CLAUDE.md》的**按需补充**，不常驻加载。
> 遇到下列场景时，把它连同《CLAUDE.md》一起给 AI。

| 场景 | 看哪节 | 配套提示词 |
|---|---|---|
| 全新仓库、这个方向第一次做、缺设计稿 | 第 1 节 | 模板 0 |
| 紧急问题排查 | 第 2.1 节 | 模板 11 |
| 性能优化 | 第 2.2 节 | 模板 12 |
| 架构调优 / 重构 | 第 2.3 节 | 模板 13 |

---

## 1. 从零开始（仓库里没有参照物）

《CLAUDE.md》的所有原则都默认「仓库里有相似实现可搜」。当这个前提不成立时，规则要相应调整，**而不是放弃规则**。

### 1.1 先分清是哪种「零」

| 情况 | 特征 | 可用锚点 |
|---|---|---|
| 全新仓库 | 连目录结构和构建脚本都没有 | 官方脚手架、团队其他仓库 |
| 新类型功能 | 有骨架，但这个方向是第一次做 | 同技术栈的开源实现、官方文档范式 |
| 缺同类实现 | 有设计稿，但仓库里搜不到可参考的写法 | 设计稿 + 当前版本官方文档 |
| 缺设计稿 | 有代码可参考，但没有界面/接口设计 | 同类产品的实际交互、可访问性基线 |

四种情况对策不同，先判断属于哪种，再往下走。

### 1.2 替代锚点从哪来（按优先级）

1. **官方脚手架 / 生成器** —— 框架自带的，优先用，别手写目录结构；
2. **当前大版本的官方文档** —— 注意版本，训练数据里的写法可能已废弃；
3. **团队其他仓库** —— 本地能找到的、最接近你们习惯的；
4. **成熟开源项目的同类模块** —— 核对它的版本和活跃度；
5. **通用最佳实践** —— 最弱，只能兜底。

**每引入一个外部锚点，在方案里标注来源和选它的理由。**
外部引入的范式必须显式标记，不能写得像是仓库既有约定。

### 1.3 规则怎么调整

对照《CLAUDE.md》第 1 节的四条原则：

| 原则 | 从零时改为 |
|---|---|
| 1.1 不发明 | 不**无标注地**发明：可以引入外部范式，但必须标明出处与理由 |
| 1.3 先搜后写 | 先搜；搜不到就**明说搜不到**，再给替代方案 |
| 1.2 报冲突 | 冲突对象从「仓库现状」变成「团队偏好」，blocker 会变多，这是正常的 |
| 1.4 声明边界 | 边界里额外加一条：**本期不引入什么**（防止顺手加中间件、抽象层） |

**不要为了消掉 blocker 而假装有约定。** 从零开始时 blocker 多是信息量大的表现，不是方案质量差。

### 1.4 第一步不是写功能，是立骨架

从零开始时，第一个阶段不应是「读懂现状」，而应是「立骨架 + 定约定」：

1. 用官方脚手架生成**最小可运行**骨架，不要手写目录结构；
2. 跑通构建 + 测试 + 启动——空跑也要通；
3. 写一份最小约定文档（结构、命名、错误处理、配置、测试放哪）；
4. **人工 review 骨架**，通过后再往上堆功能。

跳过这一步，AI 会一口气生成整个项目：你无法 review，后面也没有锚点，每加一个功能都要重新决定一次结构。

### 1.5 从零开始的特有风险

| 风险 | 表现 | 对策 |
|---|---|---|
| 训练数据污染 | AI 用「最常见」的写法，可能是几年前的、或另一个生态的 | 要求它说明写法出处，并对照当前版本官方文档 |
| 一次生成过多 | 一个 PR 几千行，review 形同虚设 | 骨架和功能分两次提交；功能再按阶段切 |
| 锚点缺失累积 | 每加一个功能都要重新决定一次结构 | 骨架阶段就把约定固化成文档 |
| 过度设计 | 提前引入用不上的抽象、中间件、设计模式 | 边界里明确写「本期不引入什么」 |
| 版本过时 | 用了已废弃的 API 或已更名的包 | 明确要求核对当前大版本 |

### 1.6 缺设计稿时

- 先做低保真——文字线框、接口字段表、状态流转表——review 后再写代码；
- 未定的交互细节列为待确认，**不要让 AI 自由发挥**；
- 定几个硬约束兜底：错误态、空态、加载态、可访问性、小屏/窄屏。

---

## 2. 排查与调优场景的附加约束

> **紧急问题排查、性能优化、架构调优**这三类工作，和「按需求做功能」是不同的范式。
> 下面每一条都是**附加**在《CLAUDE.md》核心规则之上的，不替代它们。

### 2.0 三者的共同纪律

| 场景 | 一句话铁律 |
|---|---|
| 紧急排查 | **先止血，再找根因**——不确定根因前，只做可逆的最小动作 |
| 性能优化 | **先测，再改，再测**——没有基线数据的优化一律不接受 |
| 架构调优 | **行为不变**——重构不是重写，外部行为必须一致 |

共同的三条硬约束，违反任何一条即返工：

1. **证据先于动作** —— 没定位 / 没测量 / 没锁住行为之前，不写代码；
2. **一次只改一个变量** —— 否则无法归因，改好了也不知道为什么好；
3. **必须可回滚** —— 每一步都要能退回去，退不回去就先设计怎么退。

### 2.1 紧急问题排查

**允许的节奏**

```text
1. 保现场：先收集证据（日志、监控、堆栈、复现步骤），再做任何改动
2. 定影响：确认范围与严重程度，决定是「先止血」还是「先定位」
3. 止血：用最小、可逆的动作恢复服务（回滚 > 切流量 > 关开关 > 改代码）
4. 定位：根因分析，走「假设 → 验证」循环，不要跳步
5. 修复：针对根因的最小改动
6. 复盘：补测试、补监控、留下记录
```

**禁止**

- ❌ 没定位根因就大范围改代码；
- ❌ 用吞异常、加 `try/catch`、注释掉出错逻辑的方式「让它跑起来」；
- ❌ 用 `sleep` / 盲目重试 / 扩大超时来掩盖时序或依赖问题；
- ❌ 把「应该是缓存问题 / 应该是并发问题」当作结论，那只是猜测；
- ❌ 删除或覆盖现场证据（日志、dump、复现环境）；
- ❌ 改完不记录改了什么——紧急改动最容易丢失上下文。

**必须**

- ✅ 每个动作前说明：这能**验证什么假设**、或能**恢复什么**；
- ✅ 止血优先选可逆手段，改数据是最后选项；
- ✅ 无法复现就明说「无法复现」，**不要编造一个听起来合理的解释**；
- ✅ 逐步记录「改了什么 → 观察到什么」，最终形成时间线；
- ✅ 事后补一条能提前发现该问题的监控或测试。

> **AI 自查**：你给出的根因，有**直接证据**支持吗？还是只是「符合现象」？这两者的差别，就是排查和瞎猜的差别。

### 2.2 性能优化

**铁律：先测量，后优化。**

```text
1. 定义指标：优化什么？延迟 P50/P99、吞吐、内存、CPU、IO、包体积、首屏…
2. 测基线：在**可重复**的条件下测，记录数字和环境
3. 找瓶颈：用 profiler / 火焰图 / 埋点，不靠猜、不靠"看起来慢"
4. 改一处：一次只动一个变量
5. 再测：同条件复测，对比数字
6. 判定：收益达标吗？代价是什么？值不值？
7. 保留或回滚：不达标就回滚，不要留着「以后可能有用」
```

**禁止**

- ❌ 没有基线数据就开始优化；
- ❌ 优化没被测量到的地方；
- ❌ 一次改多处，无法归因；
- ❌ 用可读性 / 可维护性换微小收益，却不说明代价；
- ❌ 加缓存却不定义失效策略、不加穿透与击穿防护；
- ❌ 把 bug 当性能问题——**先确认功能正确，再谈快慢**；
- ❌ 只在开发机测过就声称线上提升。

**必须**

- ✅ 给出 before / after 的**具体数字和环境**，不是「明显变快了」；
- ✅ 说明代价：复杂度、内存、一致性风险、可维护性；
- ✅ 优化后跑完整测试，确认行为等价；
- ✅ 收益不达标或代价过高时，**主动建议回滚**；
- ✅ 测量表明瓶颈在别处（网络、数据库、外部服务）时如实说明，**不要硬优化当前这段代码**。

> **AI 自查**：这个优化，你能拿出两个数字吗？拿不出，就不是优化，是改动。

### 2.3 架构调优 / 重构

**铁律：外部行为不变。**

```text
1. 锁定行为：先补特征化测试（characterization test），
   把**现状**——包括那些看起来怪异的行为——固定下来
2. 划清契约：哪些是对外契约（API、事件、数据结构、错误码、文件格式），
   哪些是纯内部实现
3. 小步改：每次只做一个独立的重构动作，每步后跑测试
4. 每步可回滚：一个提交一个意图，能单独回退
5. 收尾：清掉过渡代码，更新文档
```

**禁止**

- ❌ 借「重构」之名改变外部行为；
- ❌ 一个提交里做多个不相关的重构动作；
- ❌ 在没有测试保护的情况下大范围改结构；
- ❌ 顺手做性能优化——那是 2.2 的事，要单独走流程；
- ❌ 顺手改格式、改命名、动无关文件，污染 diff 让 review 失效；
- ❌ 破坏对外契约却不标注为破坏性变更。

**必须**

- ✅ 动手前先确认：现有测试覆盖了要改的部分吗？没有就**先补测试**；
- ✅ 明确列出「不变的部分」：对外契约、数据格式、错误语义、时序保证；
- ✅ 每步之后跑测试，失败就立即回滚该步，不要「改完一起修」；
- ✅ 保持 diff 可读：一个提交一个意图；
- ✅ 若发现必须改变行为，**停下来列为决策点**，不要自己决定。

> **AI 自查**：这次改动，外部观察者能看出区别吗？如果看不出，你才在做重构；如果看得出，你在做功能变更，请走正常流程。

### 2.4 三类场景共用的动手前自检

- [ ] 我手里是**证据**，还是**猜测**？
- [ ] 这一步**怎么回滚**？
- [ ] 如果我判断错了，后果是什么？可逆吗？
- [ ] 我是不是**一次改了多个变量**？
- [ ] 我在解决被问到的那个问题，还是顺手做了别的？
- [ ] 我用什么**客观数字或测试**来证明这一步有效？

> **拿不出证据 → 去拿证据，别写代码。**
> **拿不出回滚方案 → 先设计回滚，再动手。**
