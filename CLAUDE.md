# AI 编码协作规范（常驻规则）

> 本文件是**常驻规则**，适用于本项目所有 AI 编码会话。可直接拷进项目根目录的 `CLAUDE.md`，
> 或存为 `.claude/rules/ai-coding.md`。
>
> 配套文件（同目录）：
> - `README.md` —— 怎么用、工作节奏、项目适配区（第一次用先填）
> - `场景补充.md` —— 从零开始、紧急排查、性能优化、架构调优（**按需**加载）
> - `提示词模板.md` —— 给人复制粘贴的提示词
> - `检查清单.md` —— 给人 review 用的清单

---

## 第 0 步：先判断场景与规模

**同一个原则，三种强度。** 用大流程做小改动是浪费，用小流程做大改动是灾难。

**先判断场景：**

| 场景 | 走哪套 |
|---|---|
| 做新功能 | 本文档全部规则，按下面的规模裁剪方案篇幅 |
| 紧急排查 / 性能优化 / 架构调优 | 额外遵守《场景补充.md》，**那三类纪律更严** |
| 仓库无参照物、从零开始 | 额外遵守《场景补充.md》 |

**再做规模判断（仅适用于「做新功能」）：**

| 规模 | 判断标准 | 方案篇幅 | 阶段数 |
|---|---|---|---|
| **小** | 改 1–3 个文件；逻辑不跨层；单人 1 天内完成 | 半页以内 | 3（准备 / 实现 / 验证） |
| **中** | 改 3–10 个文件；落在单个模块内；需要新接口或新表 | 1–2 页 | 4–5 |
| **大** | 跨模块/跨服务/跨端；涉及数据迁移、对外契约变更、多人协作 | 完整文档 | 按依赖层级切，通常 5–8 |

**判断不了就往上一档靠。** 事后发现重了，比事后发现漏了便宜。

> **核心规则：流程可以裁剪，本文档的原则不能裁剪。**
> 小改动同样要「不发明、先搜后写、声明边界」，只是写得更短。

---

## 1. 核心原则

### 1.1 不发明

动手前，方案必须**同时**基于：

1. 项目说明与开发约定（`CLAUDE.md`、`README.md`、规范类文档、既有同类代码）；
2. 需求来源（设计文档 / 需求单 / issue / 口头约定的书面记录）；
3. **仓库中实际存在的**目录结构、命名习惯、依赖用法；
4. 明确列出的冲突、缺口、待确认问题。

**每个出现在方案里的路径、模块名、函数名、配置 key，都必须是搜到或读到的。**
凡是你"觉得应该有"的东西，先去验证；验证不了，写进待确认，不要写进文件清单。

### 1.2 冲突必须上报，禁止静默选择

需求与仓库现状冲突时，列为 **blocker**，给出 2–3 个可选方案及各自代价，**由人决策**。
不要静默挑一条会改变架构的路径，然后把方案写得像是没冲突。

### 1.3 先搜后写

写方案前，在仓库里搜索**同类功能是怎么写的**。搜什么随技术栈变，但方向固定：

```text
同类功能的完整调用链：入口 → 处理 → 存储/外部依赖 → 返回
命名习惯：文件、目录、函数、变量、测试
错误处理方式：怎么抛、怎么接、怎么给用户看
配置方式：写在哪、怎么读、有没有环境区分
测试方式：用什么框架、放在哪、怎么跑
```

**结论**：方案里应大量出现你搜到的真实路径，而不是通用模板词汇。

> 仓库里**找不到**同类实现时怎么办？见《场景补充.md》——规则要调整，但一条都不能放弃。

### 1.4 声明边界（双向）

方案里必须有两份清单：

- **做什么**：本期交付，逐条可验收；
- **不做什么**：本期明确排除的相邻功能。

第二份是**必填项**，作用是防止 AI 编码时顺手多实现。写具体，不写"其他功能暂不实现"。

---

## 2. 动手前的动作

按规模勾选，大流程包含小流程的全部动作：

| 动作 | 小 | 中 | 大 |
|---|:---:|:---:|:---:|
| 读项目说明（`CLAUDE.md` / `README.md` / 规范文档） | ✓ | ✓ | ✓ |
| 找到并精读**最相似**的已有实现（找不到则先确认替代锚点，见《场景补充.md》） | ✓ | ✓ | ✓ |
| 跑通现状（构建/测试能过）再动手 | ✓ | ✓ | ✓ |
| 定位需求来源中的目标部分（不做全文摘要） | | ✓ | ✓ |
| 搜索所有受影响的调用点 | | ✓ | ✓ |
| 读设计原型/线框图/接口文档（若有） | | ✓ | ✓ |
| 盘点外部依赖与它们的既有用法 | | ✓ | ✓ |
| 列出 blocker 并确认 | | | ✓ |
| 确认回滚方案 | | | ✓ |

**定位需求时**：先按标题、编号、接口名、表名、页面名建索引，只读相关部分。**不要对整篇文档做无差别摘要。**

---

## 3. 方案要写什么

逐项对照，`✓` 表示该规模必填。空白项可省略，但**省略要是有意的**。

| # | 部分 | 小 | 中 | 大 | 关键内容 |
|---|---|:---:|:---:|:---:|---|
| 1 | 元信息 | | ✓ | ✓ | 功能名、需求来源（路径+章节）、输出路径、日期、影响范围 |
| 2 | 目标与边界 | ✓ | ✓ | ✓ | 做什么 / **不做什么** / 前置条件 |
| 3 | 需求映射 | | ✓ | ✓ | 表格：需求条目 → 实施落点 |
| 4 | 变更清单 | ✓ | ✓ | ✓ | 逐个文件：新建或修改、路径、职责、依赖、顺序、完成标准 |
| 5 | 对外契约 | | ✓ | ✓ | 接口/函数签名/CLI 参数/事件格式：入参、出参、错误码、变更兼容性 |
| 6 | 数据与状态 | | ✓ | ✓ | schema、迁移、索引、状态机、初始化数据、回滚 |
| 7 | 界面与交互 | | ✓ | ✓ | 入口、控件、校验、加载态、错误提示、成功去向、防重复 |
| 8 | 流程与异常 | ✓ | ✓ | ✓ | 主流程 + 失败分支 + 边界条件 + 并发/幂等 |
| 9 | 安全 | ✓ | ✓ | ✓ | 见第 6 节 |
| 10 | 配置 | | ✓ | ✓ | 每项：位置、key、默认值、是否敏感、是否可提交 |
| 11 | 执行阶段 | ✓ | ✓ | ✓ | 见第 4 节 |
| 12 | 执行提示词 | ✓ | ✓ | ✓ | 见《提示词模板.md》 |
| 13 | 验收清单 | ✓ | ✓ | ✓ | 见《检查清单.md》C 节 |
| 14 | 风险与冲突 | | ✓ | ✓ | blocker 与待确认项，每条给选项 |

**小规模的方案可以就是一段话**，但必须包含 2、4、8、9、11、12、13。

> 第 7、10 项若确实不涉及（如纯后端库、无配置项），写「不适用」并说明一句，不要留空。

---

## 4. 执行阶段

### 切分原则

**按依赖顺序切，不按文件类型切。** 后一阶段只依赖前一阶段的产出。

```text
小：准备（读懂现状）→ 实现（一次改完）→ 验证（跑通 + 自测）

中：准备 → 契约/数据 → 实现 → 集成 → 验证

大：按项目的实际分层切，如
    · Web 全栈：契约 → 数据层 → 业务层 → 接口层 → 界面层 → 联调 → 验证
    · 库 / SDK：API 设计 → 实现 → 单元测试 → 文档 → 兼容性验证
    · 数据管道：schema → 抽取 → 转换 → 校验 → 调度 → 回填
    · CLI 工具：参数 → 核心逻辑 → 输出格式 → 打包 → 端到端验证
```

上面只是示例形态，**照抄前先看你的项目实际怎么分层**。

### 每个阶段必须包含

| 要素 | 说明 |
|---|---|
| 目标 | 这一阶段交付什么 |
| 输入 | 开工前要读什么 |
| 输出 | 产出/修改哪些文件 |
| **允许修改范围** | 白名单 |
| **禁止修改范围** | 黑名单——**缺这项 AI 必然越权** |
| 完成标准 | 怎么算做完，可客观判定 |
| 验证命令 | 具体命令，不是"跑一下测试" |
| 卡住怎么办 | 停下上报，还是换方案继续 |

### 执行纪律

1. 按顺序执行，不跳步、不合并阶段；
2. 每个阶段结束**停下来报告**，等确认再进下一个；
3. 遇到需求与代码冲突 → **立即停止并上报**，不要自行拍板；
4. 发现方案本身有错 → 先改方案，再改代码。

---

## 5. 阶段回执格式

```text
阶段 {N} 完成报告
- 变更文件：{路径}（新建/修改）、{路径}（...）
- 验证命令：{实际执行的命令}
- 验证结果：{实际输出摘要，不通过就写不通过}
- 遗留问题：{无 / 具体问题}
- 是否进入下一阶段：{是/否，否的话说明原因}
```

**证据先于结论。没跑过的验证命令，不要写「已通过」。**
跑失败了照实说，附上真实报错——隐瞒失败比失败本身代价大得多。

---

## 6. 安全基线（通用，与技术栈无关）

- 不硬编码凭据、密钥、token；仓库里不出现真实凭据；
- 敏感配置走环境变量或配置中心，并确认已被忽略规则排除；
- 一切外部输入都要在**服务端/可信边界内**校验，不依赖前端校验；
- 数据访问使用参数化，不拼接语句；
- 权限检查在服务端，且在**数据访问之前**；
- 敏感值不外泄：不返回给前端、不落日志、不进错误信息；
- 日志/输出脱敏（手机号、邮箱、身份证、token、卡号）；
- 错误信息不泄露内部细节（堆栈、SQL、内部路径、依赖版本）；
- 外部调用设置超时、重试上限、失败降级；
- 破坏性操作（删数据、覆盖文件、发布）需确认或支持 dry-run；
- 涉及加密/随机数/哈希时使用标准库，不自造算法。

> 按项目性质追加：若是公开 API，补充鉴权与限流；若处理个人信息，补充合规要求；若是本地工具，补充文件系统权限。

---

## 7. 禁止事项

- ❌ 不实现「不做什么」清单里的内容；
- ❌ 不新建未确认的模块、目录、依赖；
- ❌ 不修改阶段黑名单里的文件；
- ❌ 不留 `TBD` / `TODO` / `后续补充` / `视情况` 等模糊占位；
- ❌ 不把冲突写成"假设"藏在方案里；
- ❌ 不写没搜到过的路径或符号名；
- ❌ 不在验证没跑的情况下声称通过；
- ❌ 不为了让方案"看起来完整"而编造细节——宁可写「待确认」。

---

## 8. 代码质量与工程实践

> 这一节适用于**所有**场景——功能开发、排查、优化、重构。
> 第 6 节安全是底线，这一节是及格线。

### 8.1 三条排序原则

原则冲突时，按这个顺序取舍：

```text
正确 > 一致 > 简洁 > 优雅
```

1. **正确**：先能工作，行为符合预期；
2. **一致**：与仓库现有写法保持一致——**最难，也最重要**；
3. **简洁**：能少写就少写，但不是压缩行数；
4. **优雅**：最后才谈。

> **对 AI 的特别说明**：你对「优雅」的判断来自训练数据，可能与本仓库既有风格冲突。
> **当「通用最佳实践」与「本仓库现有写法」冲突时，选后者。**
> 确实认为本仓库写法有问题，就列为建议，不要直接改。

### 8.2 可读性与一致性

- 命名说**意图**，不说实现（`activeUsers` 优于 `filteredList`）；
- 沿用仓库既有命名习惯：大小写、缩写、前缀、单复数；
- 一个函数做一件事，能一句话说清；
- 嵌套超过 3 层就抽出来；
- 注释解释**为什么**，不解释**是什么**；
- 删掉死代码，不要注释掉留着（git 有历史）；
- 不留未使用的参数、变量、导入；
- 新代码格式与相邻代码一致，**不顺手格式化整个文件**。

### 8.3 错误处理

- **不吞异常**：catch 之后必须有处理——恢复、转换、上报，不能空着或只打日志继续；
- 捕获范围要**具体**，不用 `catch (Exception)` 兜底全局；
- 不用异常做控制流；
- 失败要**显式**：不要静默返回 `null` / 空集合 / 默认值来掩盖错误；
- 异常信息带**上下文**：哪个操作、哪个 id、什么参数；
- 区分可恢复与不可恢复：可恢复的降级，不可恢复的直接失败；
- 资源要释放（文件、连接、锁），用语言惯用方式（`try-with-resources` / `defer` / `with` / `using`）；
- **不要过度防御**：不给不可能为空的内部值判空，不给不会失败的内部调用包 try-catch。

> 判断标准：**如果一段错误处理去掉后程序行为没变化，它就是噪音，删掉。**

### 8.4 依赖与复用

- **引入新依赖前先搜**：仓库里是否已有等价能力？标准库是否已够用？
- 引入新依赖必须说明：为什么需要、体积、许可证、维护状态、有无替代；
- **优先复用既有工具函数 / 组件 / 类型**，不要重造；
- 不为复用而抽象——**两个相似用例不构成抽象的理由**；
- 不擅自升级依赖版本（那是独立的事）。

### 8.5 测试

- 新逻辑要有测试；**修 bug 先写能复现的测试**，修完测试要留着；
- 测**行为**，不测实现细节（否则一重构就挂）；
- 断言要能失败——避免恒真断言（断言非 null 却不检查值）；
- 至少覆盖一个失败分支和一个边界，不只测 happy path；
- **绝不为让测试通过而修改测试**——除非确认测试本身写错了，并说明理由；
- 不跳过、不注释、不 `@Ignore` 掉失败的测试来「通过」。

### 8.6 变更纪律

- **一个提交一个意图**：重构与功能不混，修复与格式化不混；
- 每次提交可单独回滚；
- 大范围改动加开关或灰度，先能关掉再上线；
- 破坏性变更必须标注（对外契约、数据格式、行为语义）；
- 不提交：生成物、构建产物、IDE 配置、临时文件、调试代码；
- 提交信息说明**为什么**改，不重复「改了什么」（diff 已经说了）。

### 8.7 AI 高频坏习惯（逐条自查）

| 坏习惯 | 表现 | 正确做法 |
|---|---|---|
| **幻觉 API** | 调用不存在的方法、参数顺序错、用了旧版 API | 写入前在仓库/文档核实该符号存在且签名正确 |
| **风格漂移** | 新代码一种风格，周围另一种 | 先读 2–3 个相邻文件，照着写 |
| **过度抽象** | 一个用例造出接口 + 工厂 + 策略 | 直接写，等第三个用例出现再抽象 |
| **过度防御** | 到处判空、到处 try-catch | 只在真实边界防御（外部输入、IO、网络） |
| **复制粘贴** | 相似逻辑复制三份 | 第三次出现时抽取 |
| **重写无关代码** | 顺手「改进」了没让你动的部分 | 只改该改的，其余列为建议 |
| **注释代替删除** | 注释掉旧代码留着 | 删掉 |
| **预留未来** | 加「以后可能用」的参数 / 开关 | 现在不用就不写，需要时再加 |
| **假测试** | 断言恒真、只测 happy path、测实现细节 | 见 8.5 |
| **假完成** | 代码里留 TODO / FIXME / 空实现 | 要么做完，要么明确上报未完成 |
| **静默降级** | 出错返回空结果，调用方不知道 | 显式抛出，或返回明确的错误类型 |
| **格式污染** | 顺手格式化整个文件，diff 无法 review | 只格式化改动所在的区块 |

### 8.8 动手前自检

- [ ] 我读过相邻的 2–3 个文件，新代码风格与之一致吗？
- [ ] 我调用的每个 API / 函数 / 配置项，都核实过存在吗？
- [ ] 我引入新依赖了吗？理由充分吗？
- [ ] 我的错误处理，去掉会影响行为吗？（不影响就该删）
- [ ] 我改的文件，都在任务范围内吗？
- [ ] 有没有注释掉的代码、TODO、调试打印留在里面？
- [ ] 测试真的能失败吗？（把实现改坏，测试会红吗）
- [ ] 这个 diff，别人能在 10 分钟内 review 完吗？
